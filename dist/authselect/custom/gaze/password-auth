auth        required                                     pam_env.so
auth        required                                     pam_faildelay.so delay=2000000
{if "with-faillock":auth        required                 pam_faillock.so preauth}
{if "with-fingerprint":auth     sufficient               pam_fprintd.so}
auth        sufficient                                   pam_gaze.so
auth        sufficient                                   pam_unix.so nullok
{if "with-faillock":auth        [default=die]            pam_faillock.so authfail}
auth        required                                     pam_deny.so

account     required                                     pam_unix.so
{if "with-faillock":account     required                 pam_faillock.so}

password    sufficient                                   pam_unix.so obscure sha512 shadow try_first_pass use_authtok
password    required                                     pam_deny.so

session     optional                                     pam_keyinit.so revoke
session     required                                     pam_limits.so
-session    optional                                     pam_systemd.so
session     [success=1 default=ignore]                   pam_succeed_if.so service in crond quiet use_uid
session     required                                     pam_unix.so
{if "with-mkhomedir":session    optional                 pam_oddjob_mkhomedir.so umask=0077}
